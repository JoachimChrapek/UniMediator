using UnityEngine;

namespace FazApp.UniMedatior
{
    public class IMediator : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
